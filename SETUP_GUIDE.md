# 🚀 Руководство по запуску системы ботов

Данное руководство поможет вам запустить полную систему из двух ботов для университетского кафе.

## 📱 Два бота в системе:

### 1. **Основной бот** (`main.py`) - для студентов
- 🍽 Просмотр меню с фотографиями
- 🛒 Заказ еды с корзиной
- 📊 История заказов

### 2. **Админ-бот** (`admin_bot.py`) - для управления
- 📋 Управление активными заказами
- 🍽 Добавление/редактирование меню
- 📸 Загрузка фотографий блюд
- 📈 Просмотр статистики

## 🛠 Пошаговая настройка:

### Шаг 1: Создание ботов в Telegram

1. **Основной бот для студентов:**
   - Найдите @BotFather в Telegram
   - Отправьте `/newbot`
   - Назовите бота, например: "University Cafe Bot"
   - Скопируйте токен

2. **Админ-бот для управления:**
   - Снова обратитесь к @BotFather
   - Создайте второго бота: "Cafe Admin Bot"
   - Скопируйте второй токен

### Шаг 2: Получение ID администраторов

1. Отправьте сообщение боту
2. Перейдите: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите ваш `chat.id`

### Шаг 3: Настройка файла `.env`

```env
# Токены ботов
BOT_TOKEN=ваш_основной_токен_здесь
ADMIN_BOT_TOKEN=ваш_админ_токен_здесь

# ID администраторов
ADMIN_CHAT_ID=ваш_id
ALLOWED_ADMIN_IDS=ваш_id,id_другого_админа

# База данных
DATABASE_URL=postgresql+asyncpg://username:password@localhost/cafe_bot
LOG_LEVEL=INFO
```

### Шаг 4: Подготовка базы данных

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Обновите структуру БД (добавит поле для фотографий)
python migrate_db.py

# 3. Заполните меню
python populate_db.py
```

### Шаг 5: Запуск системы

Откройте **два терминала**:

**Терминал 1 - Основной бот:**
```bash
python main.py
```

**Терминал 2 - Админ-бот:**
```bash
python admin_bot.py
```

## 🎯 Как пользоваться:

### Для студентов (основной бот):
1. `/start` - начать работу
2. "🍽 Меню" - просмотр и заказ
3. "🛒 Мой заказ" - управление корзиной
4. "👤 Мои заказы" - история

### Для администраторов (админ-бот):
1. "📋 Активные заказы" - обработка заказов
2. "🍽 Управление меню" - добавление/редактирование
3. "📈 Статистика" - аналитика
4. "👥 Пользователи" - информация о клиентах

## 📸 Добавление фотографий:

1. В админ-боте выберите "🍽 Управление меню"
2. "➕ Добавить позицию"
3. Заполните данные и загрузите фото
4. Фото автоматически появится в основном боте

## 🔄 Обслуживание:

### Очистка данных:
```bash
python clear_db.py
```

### Обновление меню:
```bash
python populate_db.py
```

## 🆘 Решение проблем:

### Бот не отвечает:
- Проверьте токены в `.env`
- Убедитесь, что боты запущены (`/start` у @BotFather)

### Ошибки с фотографиями:
- Проверьте права доступа к файлам
- Убедитесь, что выполнена миграция БД

### База данных:
- Проверьте подключение PostgreSQL
- Убедитесь в правильности DATABASE_URL

## 📊 Мониторинг:

Логи обоих ботов выводятся в терминал. Следите за:
- Новыми заказами
- Ошибками подключения
- Статистикой использования

## 🔐 Безопасность:

- ID администраторов задаются в конфигурации
- Админ-бот доступен только указанным пользователям
- Токены хранятся в `.env` файле (не публикуйте его!)

---

**Система готова к работе! 🎉**

При возникновении вопросов проверьте логи в терминале или обратитесь к документации.
